services:
  agent:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./soul.md:/app/soul.md
      - ./identity.md:/app/identity.md
    restart: unless-stopped
